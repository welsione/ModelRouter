# 开发规范问题报告

> 本报告列出项目中发现的开发不规范、不优雅的地方，按模块分类
> 
> **最后更新: 2026-03-10 (第三次复查 - 代码无变化)**

---

## 一、安全问题 (Security Issues) 🔴

### 1.1 认证与授权

| 序号 | 位置 | 问题描述 | 严重程度 | 建议修复 |
|------|------|----------|----------|----------|
| 1 | `server/index.js:834-847` | `/api/system/restart` 端点无任何认证保护 | **严重** | 添加认证或移除此功能 |
| 2 | 全局 | 所有API端点可任意访问，无认证机制 | **严重** | 添加API Key认证或JWT |
| 3 | 全局 | 无速率限制，容易被滥用 | **高** | 添加 rate limiting 中间件 |

### 1.2 数据安全

| 序号 | 位置 | 问题描述 | 严重程度 | 建议修复 |
|------|------|----------|----------|----------|
| 4 | `server/data/config.json` | API Key明文存储 | **严重** | 使用环境变量或加密存储 |
| 5 | `desktop-ui` | API Key在network中明文传输 | **中** | 使用HTTPS或后端代理 |

### 1.3 输入安全

| 序号 | 位置 | 问题描述 | 严重程度 | 建议修复 |
|------|------|----------|----------|----------|
| 6 | 全局 | 所有API端点缺少输入验证，req.body直接使用 | **高** | 添加Joi/Zod等验证库 |
| 7 | `desktop-ui/ChatTest.js:188` | dangerouslySetInnerHTML存在XSS风险 | **严重** | 使用安全的HTML渲染库或转义 |
| 8 | `server/db.js` | 文件路径拼接未验证，存在路径遍历风险 | **高** | 添加路径验证和过滤 |

### 1.4 代码安全

| 序号 | 位置 | 问题描述 | 严重程度 | 建议修复 |
|------|------|----------|----------|----------|
| 9 | `desktop-ui/App.js:40` | ipcRenderer.on未清理，存在内存泄漏风险 | **中** | useEffect中返回cleanup函数 |
| 10 | `server/db.js` | 使用同步文件操作，可能阻塞事件循环 | **低** | 改用异步I/O |

---

## 二、业务逻辑问题 (Business Logic Issues) 🔴

### 2.1 严重Bug

| 序号 | 位置 | 问题描述 | 影响 | 状态 |
|------|------|----------|------|------|
| 1 | `server/index.js:684` | `logs.slice(-10000)` 无赋值，日志截断无效 | 日志无限增长，内存泄漏 | 🔴 未修复 |
| 2 | `server/index.js:422-431` | 策略选择未检查 `enabled` 状态 | 禁用策略可能被使用 | 🔴 未修复 |
| 3 | `server/index.js:461` | AI路由返回的modelId可能不存在于candidates | selectedCandidate为null | 🔴 未修复 |

### 2.2 数据一致性问题

| 序号 | 位置 | 问题描述 | 影响 |
|------|------|----------|------|
| 4 | `server/index.js:106` | getData和setData之间无锁，并发风险 | 多请求可能导致数据覆盖 |
| 5 | `server/index.js:540-570` | 流式响应中多次调用db.setData | 可能出现不一致状态 |
| 6 | `server/index.js:689-697` | 更新model后未重新获取data | 可能用旧data覆盖 |

### 2.3 边界情况处理

| 序号 | 位置 | 问题描述 | 影响 |
|------|------|----------|------|
| 7 | `server/index.js:414` | `requestedModel` 解构后未使用 | 浪费内存，无功能 |
| 8 | `server/index.js:485-491` | 多模态消息处理未实现 | 图片/音频消息无法处理 |
| 9 | `server/index.js:325-328` | 路由模型未配置时直接throw | 无友好提示 |

---

## 三、功能缺陷 (Functional Issues) 🟡

### 3.1 前端问题

| 序号 | 位置 | 问题描述 | 建议修复 |
|------|------|----------|----------|
| 1 | `Logs.js:146` | 使用index作为React key | 使用log.id |
| 2 | `Models.js:17,140` | 前端用`modelId`，后端用`models`，字段不一致 | 统一字段名 |
| 3 | `Settings.js:111-129` | 自动保存无防抖，频繁请求 | 添加debounce |
| 4 | `Strategies.js:131-139` | 候选模型保存无错误处理 | 添加错误提示 |

### 3.2 Electron问题

| 序号 | 位置 | 问题描述 | 建议修复 |
|------|------|----------|----------|
| 5 | `electron/main.js:20,29` | 开发模式引用不存在的 `gateway-core` 目录 | 改为 `../server` |
| 6 | `electron/main.js:63,118` | 引用不存在的图标路径 | 改为本地路径 |
| 7 | `electron/main.js:185` | 硬编码8000ms等待后端启动 | 实现健康检查轮询 |

### 3.3 后端问题

| 序号 | 位置 | 问题描述 | 建议修复 |
|------|------|----------|----------|
| 8 | 全局 | 未捕获全局异常 | 添加 `process.on('uncaughtException')` |
| 9 | 全局 | 未捕获Promise rejection | 添加 `process.on('unhandledRejection')` |
| 10 | `server/index.js:79-91` | fetch请求无超时控制 | 添加timeout配置 |

---

## 四、代码质量问题 (Code Quality) 🟡

### 4.1 代码结构

| 序号 | 位置 | 问题描述 | 建议修复 |
|------|------|----------|----------|
| 1 | `server/index.js:853` | 单一文件850+行，过于庞大 | 按功能拆分为路由/服务/中间件 |
| 2 | `desktop-ui/Settings.js:739` | 文件680+行，过于庞大 | 拆分为多个设置区块组件 |
| 3 | `desktop-ui/Models.js:530` | 文件500+行，组件过大 | 拆分为子组件 |
| 4 | `desktop-ui/StrategyDetail.js:792` | 文件790+行，过于庞大 | 拆分为子组件 |

### 4.2 代码复用

| 序号 | 位置 | 问题描述 | 建议修复 |
|------|------|----------|----------|
| 5 | 多处 | fetchData函数在每个页面重复定义 | 提取为useFetch自定义Hook |
| 6 | 多处 | 颜色获取逻辑(getTypeColor等)重复 | 提取为工具函数 |
| 7 | 多处 | 模型获取逻辑重复 | 提取为共享Hook |
| 8 | 全局 | 无统一API请求封装 | 创建apiClient实例 |

### 4.3 代码风格

| 序号 | 位置 | 问题描述 | 建议修复 |
|------|------|----------|----------|
| 9 | 全局 | 错误响应格式不一致 | 统一错误响应格式 |
| 10 | 全局 | 无请求日志/访问日志 | 添加Morgan日志中间件 |
| 11 | 全局 | 大量硬编码数值 | 提取到配置文件 |
| 12 | 全局 | 缺少TypeScript类型定义 | 添加TypeScript |
| 13 | 全局 | 无 ESLint/Prettier 配置 | 添加代码规范工具 |

---

## 五、功能优化建议 (Enhancements)

### 5.1 路由策略功能

| 序号 | 问题 | 优化建议 |
|------|------|----------|
| 1 | `requestedModel` 未使用 | 支持客户端指定模型: `model=xxx` 直接路由 |
| 2 | 策略优先级不明确 | 增加策略优先级字段 |
| 3 | 不支持权重随机选择 | 增加轮询/随机加权选择策略 |
| 4 | AI路由无重试 | 增加AI路由重试逻辑 |
| 5 | 不支持条件路由 | 增加条件匹配规则 |

### 5.2 流式响应功能

| 序号 | 问题 | 优化建议 |
|------|------|----------|
| 6 | SSE错误处理不足 | 增强错误处理，支持错误事件推送 |
| 7 | routing信息只在首chunk返回 | 每次chunk或单独事件返回routing |
| 8 | 不支持中断/取消 | 支持客户端取消请求 |

### 5.3 模型管理功能

| 序号 | 问题 | 优化建议 |
|------|------|----------|
| 9 | 模型无启用/禁用 | 增加模型启用状态 |
| 10 | 无模型分组/标签 | 增加模型标签/分组 |
| 11 | 统计图表不丰富 | 增加时间范围筛选 |

### 5.4 日志与统计

| 序号 | 问题 | 优化建议 |
|------|------|----------|
| 12 | 日志无导出功能 | 支持导出CSV/JSON |
| 13 | 日志无时间范围筛选 | 增加日期范围筛选 |
| 14 | 统计数据不实时 | 添加WebSocket/SSE实时推送 |
| 15 | 日志存储无压缩归档 | 日志文件定期压缩归档 |

### 5.5 设置与配置

| 序号 | 问题 | 优化建议 |
|------|------|----------|
| 16 | 配置无版本管理 | 配置变更历史/回滚 |
| 17 | 无配置导入/导出 | 配置文件导入导出 |
| 18 | 无多环境配置 | dev/staging/prod切换 |

### 5.6 用户体验

| 序号 | 问题 | 优化建议 |
|------|------|----------|
| 19 | 对话无持久化 | 支持保存/加载对话历史 |
| 20 | 无法选择策略测试 | 聊天界面支持策略选择 |
| 21 | 无快捷键支持 | 添加键盘快捷键 |
| 22 | 无多语言支持 | i18n国际化 |

### 5.7 API兼容性

| 序号 | 问题 | 优化建议 |
|------|------|----------|
| 23 | 不支持model参数 | 兼容OpenAI API |
| 24 | 不支持function calling | 增加tools支持 |
| 25 | 不支持JSON mode | 增加response_format |
| 26 | 不支持seed参数 | 增加确定性输出 |

### 5.8 性能优化

| 序号 | 问题 | 优化建议 |
|------|------|----------|
| 27 | 每次请求都读文件 | 增加缓存层 |
| 28 | 大数据量无分页 | 日志/统计分页加载 |
| 29 | 无请求合并/批处理 | 支持批量模型调用 |

### 5.9 监控与运维

| 序号 | 问题 | 优化建议 |
|------|------|----------|
| 30 | 无健康检查端点 | 添加 `/health` 端点 |
| 31 | 无metrics监控 | 添加Prometheus metrics |
| 32 | 无告警机制 | 异常请求/延迟告警 |

---

## 六、已修复问题汇总 ✅

| 序号 | 位置 | 原问题 | 修复时间 |
|------|------|--------|----------|
| 1 | `server/index.js:290` | `readSettings()` 未定义 | 2026-03-09 |
| 2 | `server/index.js:556` | 流式日志截断无效 | 2026-03-09 |
| 3 | `desktop-ui/Settings.js:80` | routerDefaults变量未定义 | 2026-03-09 |

---

## 七、架构设计问题

| 序号 | 问题描述 | 建议 |
|------|----------|------|
| 1 | 日志无限增长，无自动清理机制 | 添加日志轮转/清理任务 |
| 2 | 无数据库备份/导出功能 | 添加数据导出功能 |
| 3 | 无单元测试 | 添加Jest/Vitest测试 |
| 4 | server目录无README/文档 | 补充API文档 |

---

## 八、待实现功能

| 序号 | 功能 | 描述 |
|------|------|------|
| 1 | 用户认证 | 登录/权限管理 |
| 2 | API Key管理 | 为不同应用生成独立API Key |
| 3 | 使用配额 | 限制API调用次数/Token用量 |
| 4 | Webhook | 请求完成回调通知 |
| 5 | 缓存管理 | 模型响应缓存 |
| 6 | A/B Testing | 路由策略A/B测试 |
| 7 | 多租户 | 支持多用户/组织 |

---

*报告结束*
